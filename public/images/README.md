# Category images

Place your category images here for the Home page:

- `inspired.jpg` – Inspired category
- `custom.jpg` – Custom category  
- `luxury.jpg` – Luxury category

Use these exact filenames (lowercase). If a category image is missing, the first carousel image is used as fallback.
